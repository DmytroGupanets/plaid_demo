export interface IButton{
    onClick: () => void,
    isPreloader?: any,
    theme?: string,
    last?: boolean,
    isDizActiveButton?: any,
    focus?: boolean,
    isSelected?: boolean,
    style?: any,
    type?: string,
    className?: string,
}